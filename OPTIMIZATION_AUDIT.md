# ReelFin Optimization Audit

## Measured Problems

1. Critical: launch path eagerly constructs the full production container before the root view can resolve auth state.
   Files: `ReelFinApp/App/ReelFinApp.swift`, `ReelFinApp/App/AppContainer.swift`, `ReelFinApp/AppTV/ReelFinTVApp.swift`, `ReelFinApp/AppTV/TVAppBootstrap.swift`
   Impact: longer cold launch and unnecessary logged-out spinner time.
   Fix: build a synchronous `LaunchSnapshot` from persisted settings and token state, and gate foreground sync on an active session.

2. High: authenticated Home first paint waits on `sync(.appLaunch)` even when cached content exists.
   Files: `ReelFinUI/Sources/ReelFinUI/Home/HomeViewModel.swift`
   Impact: slower warm launch and slower return-to-app responsiveness.
   Fix: publish cached Home immediately, keep sync as background enrichment.

3. High: Library search/filter/sort/pagination use multiple unowned tasks and can race.
   Files: `ReelFinUI/Sources/ReelFinUI/Library/LibraryView.swift`, `ReelFinUI/Sources/ReelFinUI/Library/LibraryViewModel.swift`
   Impact: stale grid results, extra UI churn, unnecessary repeated work.
   Fix: centralize criteria changes and pagination into one generation-based latest-wins load pipeline.

4. High: tvOS focus handoff currently includes fixed sleeps in the root shell and content-entry helpers.
   Files: `ReelFinUI/Sources/ReelFinUI/TV/TVRootShellView.swift`, `ReelFinUI/Sources/ReelFinUI/Home/HomeView.swift`, `ReelFinUI/Sources/ReelFinUI/Library/LibraryView.swift`, `ReelFinUI/Sources/ReelFinUI/TV/TVSearchView.swift`
   Impact: visible focus latency and fragile navigation on Apple TV.
   Fix: notify readiness from content focus paths and shrink fixed-delay fallback windows.

5. Medium: speculative artwork warming on Home/Library/Detail primarily warms HTTP/server cache, not decoded image cache.
   Files: `JellyfinAPI/Sources/JellyfinAPI/JellyfinAPIClient.swift`, `ImageCache/Sources/ImageCache/DefaultImagePipeline.swift`, `ReelFinUI/Sources/ReelFinUI/Home/HomeView.swift`, `ReelFinUI/Sources/ReelFinUI/Library/LibraryView.swift`, `ReelFinUI/Sources/ReelFinUI/Detail/DetailViewModel.swift`
   Impact: slower first render after navigation and extra decode churn during focus/scroll.
   Fix: route speculative artwork requests through `DefaultImagePipeline.prefetch(urls:)` and deduplicate candidate URLs.

6. Medium: non-selected iOS detail cards were still rendering the full hero backdrop path.
   Files: `ReelFinUI/Sources/ReelFinUI/Detail/DetailView.swift`
   Impact: extra artwork requests, decode work, blur cost, and carousel hitch risk while swiping.
   Fix: use a lighter preview-card artwork mode for non-selected cards and reserve the full cinematic background path for the active hero.

## Inferred Problems

1. High: `DetailViewModel` season and prepared-episode tasks can outlive the current selection.
   Files: `ReelFinUI/Sources/ReelFinUI/Detail/DetailViewModel.swift`
   Impact: stale progress or warmup state can land after a newer episode or season selection.
   Fix: introduce explicit season and episode selection tokens plus cancellable preparation tasks.

2. Medium: Home and Detail still have broad invalidation surfaces.
   Files: `ReelFinUI/Sources/ReelFinUI/Home/HomeView.swift`, `ReelFinUI/Sources/ReelFinUI/Detail/DetailView.swift`
   Impact: unnecessary body recomputation during focus, scroll, and phased loading.
   Fix: split scroll and hydration state from the largest view roots in a follow-up milestone.

3. Medium: playback observer work still has lifecycle-owned-task gaps.
   Files: `PlaybackEngine/Sources/PlaybackEngine/PlaybackSessionController.swift`, `ReelFinUI/Sources/ReelFinUI/Player/NativePlayerViewController.swift`
   Impact: stale observer mutations after stop/reload remain possible.
   Fix: item-scoped observer guards and delayed validation ownership landed; keep pushing synthetic seek invalidation and remaining hot observer work into cancelable or session-scoped ownership.

4. Low: repo cleanup drift left obviously stale operational files behind.
   Files: `tasks/todo.md`, `scripts/export_marketing_screenshots.sh`
   Impact: misleading backlog state and broken maintenance entry points.
   Fix: remove files only when they are unreferenced or target already-deleted codepaths.

## Speculative Improvements

- Replace the dependency bag with narrower feature containers.
- Split `PlaybackSessionController` into lifecycle, route-planning, and reporting components.
- Unify image URL generation and speculative artwork policy behind a dedicated artwork service.

## Top 10 Ranked Fixes

1. `High-leverage architecture change` startup snapshot plus lazy heavy-service startup
2. `Quick win` gate foreground sync on authenticated session and fix UI-test reset ordering
3. `High-leverage architecture change` cached-first Home load independent from app-launch sync
4. `Quick win` Library latest-wins request ownership
5. `Medium refactor` Detail season and episode freshness tokens
6. `Medium refactor` narrower SwiftUI observing surfaces on Home and Detail
7. `Quick win` cheaper artwork for non-selected detail preview cards
8. `Quick win` tvOS content-ready focus handoff and test compile fix
9. `Medium refactor` playback observer lifecycle ownership
10. `Quick win` decoded-image speculative prefetch

## Implemented So Far

- Launch bootstrap now uses a synchronous `LaunchSnapshot` so persisted auth/onboarding state is available before `ReelFinRootView` starts async bootstrap.
- UI-test auth reset now runs before API session capture, and foreground sync is skipped when there is no active session.
- `HomeViewModel` now publishes cached/stale Home content before app-launch sync finishes and only treats launch as blocking when there is nothing renderable.
- `LibraryViewModel` now owns one generation-based latest-wins pipeline for reloads and pagination, with explicit task cancellation instead of overlapping ad hoc loads.
- `DetailViewModel` now uses season and episode freshness tokens plus owned warmup/progress tasks so stale async work cannot overwrite the current selection.
- Non-selected iOS detail carousel cards now use a lighter preview artwork path instead of paying the full hero-background cost for every card.
- tvOS focus handoff removed the largest fixed sleeps from root/search/library entry and reduced fallback delays in Home focus restoration.
- Speculative artwork warming now reaches `DefaultImagePipeline.prefetch(urls:)`, and both pipeline-level and API-level candidate URLs are deduplicated before work starts.
- tvOS live smoke tests no longer depend on unsupported `tap()` login flows; they compile and skip cleanly when the simulator lacks an authenticated session.
- `PlaybackSessionController` now cancels delayed validation, synthetic seek invalidation, and synthetic prefetch work on stop/reload, instead of letting stale delayed tasks drift across sessions.
- `NativePlayerViewController` no longer creates a new `Task` on every 150 ms trickplay preview tick.
- Removed stale repo artifacts that no longer had a valid backing workflow: `tasks/todo.md` and `scripts/export_marketing_screenshots.sh`.
- `PlaybackSessionController` now overlaps startup preheat with AVPlayer preparation and gates high-risk autoplay on bounded buffer readiness, reducing start-then-stall behavior without blocking HLS playlist startup behind byte-range probes.
- `DetailViewModel` now treats episode playback preparation as latest-wins, so stale progress or warmup selections cannot overwrite the active detail playback target.
- Added token-safe media cache keys and a durable `MediaGatewayIndex` so future player warmup and cache promotion work can reason about route, user/server scope, audio/subtitle choice, resume bucket, TTL, byte size, and LRU order without persisting raw credentials.
- Added `HLSSegmentDiskCache` as the first concrete media payload cache for playlists, init segments, and media segments, with hashed filenames, TTL expiry, LRU eviction, corrupt-index recovery, and sensitive-material regression tests.
- Added `scripts/run_zero_stall_validation.sh` as a repeatable validation runner for XcodeGen, iOS build, tvOS build, startup/detail tests, and App Store screenshot tests.
- High-bitrate iOS DirectPlay startup now keeps AVPlayer paused through replacement, seek, bounded readiness gating, and video preroll, refuses timeout-based start when measured buffer is still unsafe, uses a 24s no-stall buffer target, and only marks first frame after a real video pixel buffer when video output is attached.
- Startup DirectPlay stalls, readiness timeouts, and video-preroll failures now preserve resume ticks and recover through direct-route-disabled HLS/transcode profiles instead of replaying the same progressive DirectPlay path that just failed.
- The latest log regression (`resume=1077.5s` followed by fallback `resume=none` and first frame near 2s) is covered by tests that force a DirectPlay-capable MOV/MP4 source through recovery and assert `StartTimeTicks` plus an H.264 transcode route.
- Detail-page playback warmup now passes resume/runtime/platform context into `PlaybackWarmupManager`, which deduplicates startup media-byte preheats by route, platform, and resume bucket; high-bitrate iOS progressive DirectPlay warmup samples up to 12 MiB around the resume byte range with file-end caps, and playback startup uses the same preheater path so a warmed detail page can avoid repeating untracked probes.
- Series detail warmup now resolves Jellyfin Next Up before loading episodes, selects the matching season when the next episode is in a later season, and only falls back to first unplayed/first episode when the server has no Next Up target.
- tvOS Home, Detail, Library, Search, and top navigation now use native no-chrome `Button` activation for selectable focus surfaces instead of custom focusable views with tap gestures; Detail row-up navigation also scrolls the target section into view before applying the focus handoff.
- The earlier strict sparse-buffer fallback path is superseded because it caused false tvOS progressive Direct Play startup timeouts and transcode restarts before the first frame.
- Detail-page warmup no longer marks progressive Direct Play as fully ready from disposable URLSession range-probe bytes; the UI signal is reserved for startup paths with cache/readiness evidence AVPlayer can actually consume.
- Earlier Direct Play startup tuning prioritized sparse telemetry and skipped blocking tvOS video-preroll recovery; the post-first-frame stall suppression from that pass is superseded by the latest cut-after-start fix below.
- The previous tvOS Direct Play no-cut rule favored a single uninterrupted progressive item; the latest `MEDIA_PLAYBACK_STALL` log showed that a bounded post-start fallback is safer for visible playback continuity.
- Detail-page background playback warmup no longer drives the blocking `Preparing` state or disables tvOS Play focus; Direct Play preheat timeouts can fail silently without trapping the Detail page.
- The tvOS Direct Play startup gate no longer treats sparse `loadedTimeRanges` as unsafe for progressive streams; high-bitrate Direct Play now starts from AVPlayer `readyToPlay` instead of waiting 45s and rebuilding as transcode.
- Direct Play remains the preferred route once selected, but it is no longer mandatory after an observed post-start stall on high-bitrate/premium tvOS progressive playback.
- The earlier slow-start log (`resume=4655s`, `ready min=0 preferred=0`, then `readiness.timeout elapsed=17s`) was addressed by allowing zero-buffer Direct Play gates on lower-risk progressive assets; M22 keeps high-bitrate/premium tvOS Direct Play on a measured-buffer no-stall path.
- tvOS progressive Direct Play warmup no longer performs disposable URLSession range probes, removing a competing request that was timing out against the same Jellyfin stream while AVPlayer was trying to start.
- tvOS Direct Play no longer forces a 90s startup buffer for premium progressive files; that target made the first-frame path visibly slower and encouraged large progressive reads before playback could prove the connection was healthy.
- tvOS cache ramp decisions now use elapsed playback time since the first frame, not the resumed movie position, so resuming at `4655s` cannot immediately jump to hot/deep/flood buffering.
- Resume-based HLS/transcode no longer applies an absolute deferred seek after first frame, avoiding the post-start timebase churn seen when fallback streams began at `StartTimeTicks`.
- Progressive Direct Play warmup now skips disposable URLSession range probes on iOS as well as tvOS, so the only startup reader for remote MP4/MOV is AVPlayer itself.
- iOS high-bitrate progressive Direct Play now gates on `readyToPlay` for preroll instead of requiring measured 5-10s buffer telemetry before playback intent, reducing avoidable first-frame delay while keeping the audio/video sync guard.
- Home featured playback now presents the native full-screen player before session resolution completes, matching Detail startup behavior and making the user action feel immediate.
- The latest tvOS cut-after-start log (`DirectPlay`, `bitrate=21868794`, first frame after ~6.3s, then `MEDIA_PLAYBACK_STALL`) supersedes the earlier "Direct Play mandatory" tuning: high-bitrate/premium tvOS progressive Direct Play now uses a 24s no-stall forward-buffer target with `automaticallyWaitsToMinimizeStalling`, no longer qualifies for immediate zero-buffer readiness, and a stall within 20s of first frame recovers through direct-route-disabled HLS/transcode fallback.
- Playback item observers now guard the current `AVPlayerItem` before mutating playback state, and delayed startup subtitle selection/video validation tasks are canceled on stop, reload, replacement, and first-frame completion.
- The follow-up tvOS stall log (`bitrate=21868794`, `MEDIA_PLAYBACK_STALL` after a slow progressive start, fallback racing while old DirectPlay still emitted proof/stall telemetry) initially exposed a bad overcorrection: preemptive HLS/transcode added multi-second resolution cost and could abandon the native route. Auto-quality tvOS Direct Play now stays Direct Play when the configured streaming budget has clear headroom over the source bitrate.
- Compatible high-bitrate tvOS Direct Play skips the startup readiness delay and keeps the fast Direct Play buffer policy when network headroom is known. Apple still gets the native progressive file path first; fallback is reserved for explicit over-budget cases or real playback failure evidence.
- Startup-readiness, preroll, and preflight failures no longer replay the same progressive Direct Play route; profile recovery suspends the old player item, cancels observers/prerolls/validation/proof tasks, and then loads the fallback route with the current resume position.
- Native Matroska resume no longer depends on Cues being inside the first 8 MB. SeekHead is parsed and used for a bounded range read of late-file Cues, so resume can jump directly to the target cluster on common MKV layouts instead of scanning from the beginning.
- Native Matroska remote seeking now asks the demuxer to reposition before decoding resumes. This removes the old flush-plus-linear-skip behavior where repeated right-arrow seeks could restart buffering and walk packets until the target.
- Fallback/profile re-resolution preserves resume in the Jellyfin PlaybackInfo request, but final HLS URLs handed to AVPlayer strip `StartTimeTicks`/`StartTime`; pinned variant URLs must not inherit resume query parameters from the master playlist.
- Home and Library duplicate enrichment now score local playback quality from existing metadata instead of resolving playback sources or warming media routes during feed/list normalization, removing the repeated `playback.selection` network storm seen before the user clicked Play.
- Periodic playback progress still saves local position on every player tick, but remote Jellyfin `/Sessions/Playing/Progress` updates are throttled, latest-wins, and paused during route recovery so telemetry cannot compete with media startup/fallback traffic.
- The latest The Boys black-screen log (`audio_only_no_video` after Direct Play DV/HDR10+ advanced playback without a decoded frame) is now treated as a structured video-decode failure. It no longer reloads the same Direct Play URL; recovery suspends the old item and disables direct routes so the next candidate must be a profile fallback. `directplay_stall` remains the only same-route Direct Play recovery reason.
- Strict native mode now stays on the sample-buffer engine: compatible MP4/MOV originals and Matroska originals use the native sample-buffer surface, while `AVPlayerItem`, `AVPlayerViewController`, Jellyfin HLS/transcode URLs, and loopback local HLS are blocked by route guards.
- Player presentation now keeps one stable `native_engine_player_screen` accessibility anchor for the sample-buffer native surface.
- HDR/Dolby Vision preservation now has an explicit native pipeline instead of relying on renderer defaults: Matroska/MP4 demuxing carries HDR metadata, HEVC sample-buffer format descriptions include CoreMedia color extensions, MP4 native playback keeps compressed samples, and Direct Play/sample-buffer presentation opts into the Apple HDR display paths.
- Dynamic media cache now backs original-first playback: Direct Play originals can be served through an opaque `127.0.0.1` Range gateway, native sample-buffer reads can use a persistent byte cache, and the policy is tvOS-aggressive/iOS-moderate with cache phases from startup through complete-item caching.
- The local Direct Play gateway now gives AVPlayer priority on requested ranges, writes completed ranges atomically, and starts bounded background prefetch from the active cache policy so good tvOS/iOS networks build useful ahead cache without exposing remote URLs or headers.
- Media cache persistence now protects secrets and cache correctness: range chunks write atomically, `.part` files are ignored, TTL/LRU metadata is stored separately from payloads, server/user scope can be removed, and route signatures ignore API keys, bearer tokens, cookies, and Jellyfin token headers.
- Settings now exposes a minimal `Media Cache` control (`Automatic`, `Reduced`, `Off`) plus a clear-cache action, keeping the default user experience quality-first while preserving a reduced/off escape hatch for storage or data constraints.

## Validation Results

- Dynamic media cache validation: targeted cache/gateway/settings tests passed, including red/green coverage for gateway ahead-prefetch; `xcodegen generate`, iOS `ReelFin` simulator build, tvOS `ReelFinTV` simulator build, and `git diff --check` passed; fast live Jellyfin E2E passed with artifacts `.artifacts/player-e2e/20260505-122924` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `4/4` live playback probes, `76/76` deterministic tests, clean fatal-log scan; UI and tvOS runner steps intentionally skipped).
- `xcodegen generate`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinAppleTransmuxDD -only-testing:PlaybackEngineTests/NativeApplePlaybackRoutePlannerTests -only-testing:PlaybackEngineTests/NativePlayerRouteGuardTests -only-testing:PlaybackEngineTests/NativePlayerSessionRoutingTests -only-testing:PlaybackEngineTests/NativePlayerPlaybackControllerEndToEndTests`: passed, 19 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinAppleTransmuxDD -only-testing:PlaybackEngineTests`: passed, 442 tests, 4 expected skips
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinAppleTransmuxUITestDD3 -only-testing:ReelFinUITests/AppStoreScreenshotTests/testCapturePlayerScreenshot`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinAppleTransmuxFullIOSDD2`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2' -derivedDataPath /tmp/ReelFinAppleTransmuxFullTVDD2`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinAppleTransmuxIOSBuildDD`: historical pass before M28; sample-buffer deprecation warnings resolved in M28
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2' -derivedDataPath /tmp/ReelFinAppleTransmuxTVBuildDD`: historical pass before M28; sample-buffer deprecation warnings resolved in M28
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests`: passed, 123 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/HomeViewModelFeedEnrichmentTests/testLoadPrefersLocalPlaybackQualityWithoutWarmupWhenDuplicateCandidatesShareTheSameMovie -only-testing:PlaybackEngineTests/LibraryViewModelTests/testLoadInitialPrefersLocalPlaybackQualityWithoutResolvingSourcesAcrossLibraries`: passed, 2 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackStopReportingTests`: passed, 125 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackDecisionEngineTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests`: passed, 140 tests
- `xcodegen generate`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `git diff --check`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinBlackScreenDD -only-testing:PlaybackEngineTests/PlaybackPolicyTests/testStartupRecoveryDisablesDirectRoutesForUnsafeProgressiveFailures -only-testing:PlaybackEngineTests/PlaybackPolicyTests/testStartupFailureReasonAudioOnlyNoVideoTriggersRecovery -only-testing:PlaybackEngineTests/PlaybackPolicyTests/testStartupFailureReasonRawValueRoundTrip -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests/testStartupDirectPlayFailuresSkipSameRouteRecovery -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests/testVideoDecodeFailuresDisableDirectRouteRecovery`: passed, 5 tests
- `xcodegen generate`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackStartupPreheaterTests -only-testing:PlaybackEngineTests/PlaybackWarmupManagerTests -only-testing:PlaybackEngineTests/DetailViewModelActionTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackResumeSeekPlannerTests -only-testing:PlaybackEngineTests/PlaybackTVOSCachingPolicyTests`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1'`: passed after rerunning sequentially
- `git diff --check`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed
- `xcodegen generate`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupPreheaterTests -only-testing:PlaybackEngineTests/PlaybackWarmupManagerTests -only-testing:PlaybackEngineTests/DetailViewModelActionTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 82 tests
- `git diff --check`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1'`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodegen generate`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 60 tests
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackResumeSeekPlannerTests -only-testing:PlaybackEngineTests/PlaybackTVOSCachingPolicyTests`: passed
- `xcodegen generate`: passed for build `1.0 (6)`
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild archive -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'generic/platform=tvOS' -archivePath .artifacts/testflight/ReelFin-tvOS-b6.xcarchive DEVELOPMENT_TEAM=WZ4CHJH7TA CODE_SIGN_STYLE=Automatic`: passed
- `xcodebuild -exportArchive -archivePath .artifacts/testflight/ReelFin-tvOS-b6.xcarchive -exportPath .artifacts/testflight/export-b6 -exportOptionsPlist .artifacts/testflight/export-b5/ExportOptions.plist`: passed
- `codesign -dv --verbose=2 /tmp/reelfin-ipa-b6/Payload/ReelFin.app` plus `Info.plist` checks: passed, `com.reelfin.app`, version `1.0`, build `6`
- `asc publish testflight --app 6762079357 --ipa .artifacts/testflight/export-b6/ReelFin.ipa --platform TV_OS --version 1.0 --build-number 6 --group "Internal Testers" --wait`: passed, build `e5c4d93a-7592-4a81-bf6e-8b1496a16238`, processing state `VALID`
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackTVOSCachingPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupPreheaterTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackResumeSeekPlannerTests`: passed, 136 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupPreheaterTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests -only-testing:PlaybackEngineTests/PlaybackResumeSeekPlannerTests`: passed, 128 tests
- `xcodegen generate`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/RootViewModelAuthPersistenceTests`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/HomeViewModelActionTests`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/LibraryViewModelTests`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStopReportingTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1'`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2' -only-testing:ReelFinTVUITests/TVLiveNavigationSmokeUITests`: passed with 3 expected skips because the simulator had no persisted authenticated tvOS session
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackStartupPreheaterTests -only-testing:PlaybackEngineTests/DetailViewModelActionTests/testPrepareEpisodePlaybackLatestWinsAcrossWarmupSignals`: passed, 14 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:ReelFinUITests/AppStoreScreenshotTests`: passed, 4 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/MediaGatewayCacheKeyTests -only-testing:PlaybackEngineTests/MediaGatewayIndexTests -only-testing:PlaybackEngineTests/HLSSegmentDiskCacheTests`: passed, 13 tests
- `bash -n scripts/run_zero_stall_validation.sh`: passed
- `scripts/run_zero_stall_validation.sh`: passed, artifacts in `.artifacts/zero-stall/20260419-160313`
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 57 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: not runnable because `PlaybackEngineTests` is not a member of the `ReelFinTV` scheme/test plan
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed, 2 tests
- `xcodegen generate`: passed for build `1.0 (5)`
- `xcodebuild archive -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'generic/platform=tvOS' -archivePath .artifacts/testflight/ReelFin-tvOS-b5.xcarchive DEVELOPMENT_TEAM=WZ4CHJH7TA CODE_SIGN_STYLE=Automatic`: passed
- `xcodebuild -exportArchive -archivePath .artifacts/testflight/ReelFin-tvOS-b5.xcarchive -exportPath .artifacts/testflight/export-b5 -exportOptionsPlist .artifacts/testflight/ExportOptions-tvOS-manual.plist`: passed
- `codesign -dv --verbose=2 /tmp/reelfin-ipa-b5/Payload/ReelFin.app` plus `Info.plist` checks: passed, `com.reelfin.app`, version `1.0`, build `5`
- `asc publish testflight --app 6762079357 --ipa .artifacts/testflight/export-b5/ReelFin.ipa --platform TV_OS --version 1.0 --build-number 5 --group "Internal Testers" --wait`: passed, build `0974cb76-e710-4f90-922f-0d5731d765c7`, processing state `VALID`
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinZeroStallDerivedData -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 42 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinZeroStallDerivedData -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackDecisionEngineTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests -only-testing:PlaybackEngineTests/PlaybackPolicyTests`: passed, 124 tests
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1'`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -derivedDataPath /tmp/ReelFinDetailWarmupDerivedData -only-testing:PlaybackEngineTests/PlaybackWarmupManagerTests -only-testing:PlaybackEngineTests/DetailViewModelActionTests`: passed, 8 tests
- `xcodegen generate`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild test -quiet -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild build -quiet -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1'`: passed
- `xcodegen generate`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 57 tests
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodegen generate`: passed for build `1.0 (3)`
- `xcodebuild archive -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'generic/platform=tvOS'`: passed
- `xcodebuild -exportArchive` with `.artifacts/testflight/ExportOptions-tvOS-manual.plist`: passed
- `asc publish testflight --platform TV_OS --group "Internal Testers"`: passed, build `1.0 (3)` processing state `VALID`
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/DetailViewModelActionTests`: passed, 9 tests
- `git diff --check`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodegen generate`: passed for build `1.0 (4)`
- `xcodebuild archive -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'generic/platform=tvOS'`: passed
- `xcodebuild -exportArchive` with `.artifacts/testflight/ExportOptions-tvOS-manual.plist`: passed
- `codesign` and `Info.plist` IPA verification: passed, distribution signed, `CFBundleVersion=4`
- `asc publish testflight --platform TV_OS --group "Internal Testers"`: passed, build `1.0 (4)` processing state `VALID`, build ID `d5efc767-d12f-4aa3-b6ba-67c89b43c964`
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFin -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.3.1' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: passed, 57 tests
- `git diff --check`: passed
- `xcodebuild build -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2'`: passed, 2 tests
- `xcodebuild test -project ReelFin.xcodeproj -scheme ReelFinTV -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation),OS=26.2' -only-testing:PlaybackEngineTests/PlaybackStartupReadinessPolicyTests -only-testing:PlaybackEngineTests/PlaybackSessionControllerTrackReloadTests`: not runnable because `PlaybackEngineTests` is not a member of the `ReelFinTV` scheme/test plan

## Remaining High-Value Follow-Ups

- Split scroll-linked and hydration state out of `HomeView` and `DetailView` so focus and phased-loading updates do not invalidate large view roots.
- Continue the playback lifecycle hardening pass for observer callbacks and reload paths that still hop through ad hoc `Task` boundaries.
- Add launch/signpost regression assertions and stronger playback TTFF checks so performance assumptions stop living only in manual scripts.
- Extend the media cache with native sample-buffer background prefetch and richer network-cost signals; the Direct Play gateway path now has write-through caching and bounded ahead prefetch.

## Rejected Or Deferred Optimizations

- Replacing Apple playback with third-party media engines: dangerous and outside current product constraints.
- Rewriting SwiftUI rails into `UICollectionView`: not justified without fresh measurement after first-wave fixes.
- Replacing GRDB or rewriting DTO decoding: no current evidence that they are the top bottleneck.
- Large Xcode target-graph surgery: needs measurement first.

## Native Engine Player Performance Note - 2026-04-23

- Added range-read and native pipeline diagnostics for throughput, buffered ranges, decode/render latency fields, dropped frames, and A/V sync offset.
- Added bounded cache-window and backpressure policy foundations.
- Added original-file MP4 and Matroska sample-buffer playback through `AVSampleBufferDisplayLayer` and `AVSampleBufferAudioRenderer`, with native diagnostics proving `serverTranscodeUsed=false`.
- The first tvOS Matroska stutter log showed repeated `nativeplayer.audio.rebuffer` with `audioAhead=0` despite hundreds of audio packets. The fix now synthesizes missing Matroska audio packet durations, advances laced E-AC3 PTS in streaming clusters, normalizes duplicate compressed-audio PTS values at the renderer boundary, and requires multiple starvation ticks before pausing the render synchronizer.
- The follow-up stutter log showed `nativeplayer.buffering.ready` with `audioPackets=1` and `audioAhead=9.632`, proving the policy was counting local Swift queue duration as if it were already primed inside Apple’s audio renderer. Startup now requires 32 renderer-accepted audio samples, rebuffer resume requires 16, diagnostics expose `primed video/audio`, and the audio renderer is fed by `requestMediaDataWhenReady` instead of a fixed polling timer.
- The next runtime log showed `nativeplayer.buffering.ready` with `audioPackets=32`, `audioPrimed=32`, and `audioAhead=10.624`, moving the remaining risk to the audio timeline/renderer boundary. The Matroska audio timing normalizer now collapses forward gaps as well as duplicate/backward timestamps and reports `maxAudioPTSCorrection` in diagnostics. Audio rebuffering also moved from callback-count based gating to sustained starvation time, because `requestMediaDataWhenReady` can fire repeated ready callbacks in milliseconds and must not pause the render synchronizer for transient local queue gaps.
- The same symptom can also occur when a Matroska AC-3/E-AC-3 block stores several Dolby syncframes in one block. `CompressedAudioSampleBuilder` now parses AC-3/E-AC-3 frame sizes and creates multi-sample compressed `CMSampleBuffer`s with continuous per-frame timing instead of sending a multi-frame block as one false-duration sample.
- The latest native stutter logs showed the route was correct but playback still had brief audio+video cuts after `buffering.ready`. The player was still reapplying `AVSampleBufferRenderSynchronizer.setRate(1, time: currentTime)` on every unchanged SwiftUI configure pass caused by diagnostics/progress refreshes. MP4 and Matroska native controllers now gate pause/rate changes, do not restart playback when only live start/progress time changes for the same URL, avoid pausing both audio and video for transient audio starvation, and raise Matroska startup buffering to reduce cluster/range boundary starvation.
- The following native startup log showed a different failure: route/probe/demux/renderers started, but `buffering.ready` never appeared and the UI stayed in buffering. Startup readiness now has an explicit audio watchdog and renderer-failure path. The watchdog is independent from transient audio-starvation reset callbacks, so partial renderer callbacks cannot keep extending the startup wait forever. If video is ready but compressed audio cannot prime within the bounded startup window, or `AVSampleBufferAudioRenderer` reports failure, the player logs the exact audio blocker, degrades audio startup, and starts video instead of deadlocking indefinitely.
- Native player dismissal now stops sample-buffer renderers explicitly and forwards final native playback time to progress reporting, so leaving the player should not leave smooth audio running behind the detail screen.
- The Ready Player One-style slow-start log showed the opposite problem from earlier stutter work: the old initial Matroska gate required deep renderer priming (`requiredAudioPrimed=32`, `requiredAudioAhead=8`, `requiredVideoAhead=5`) before first play. Startup is now split from rebuffer recovery. Initial playback can start from a short renderer-prime window (`8` accepted audio samples and `1.25s` audio/video ahead), while post-start rebuffer recovery still requires deeper coverage so real starvation does not become visible cuts.
- The native player UI now uses a Liquid Glass transport overlay instead of the diagnostics wall as the primary chrome. Diagnostics remain available by toggle, and tvOS uses a focusable progress scrubber because SwiftUI `Slider` is unavailable on tvOS.
- The transport overlay is now closer to the Apple tvOS player visual hierarchy: no enclosing control card, no black scrim behind the controls, no parent black backdrop on the sample-buffer route, bottom-left episode/title block, full-width thin timeline, transparent `Glass.clear` pill actions, and round `Glass.clear` icon controls. The overlay auto-hides after a short idle delay during clean playback and stays visible for pause, buffering, diagnostics, and errors.
- The tvOS native seek path now coalesces repeated remote moves before committing a seek, keeps the target time visible during buffering, and applies forward Matroska seeks in-place on the current demuxer/HTTP stream instead of opening a new range source for every cursor movement. This directly targets logs where one scrub produced many `nativeplayer.sampleReader.start` events and long `audioPackets=0` buffering waits.
- The custom Liquid Glass chrome now disables default tvOS focus/hover halos on the pill buttons and scrubber, uses clear interactive glass rather than material-heavy blurred fills, and keeps the timeline thin so focus does not create the oversized white capsule shown in screenshots.
- The corrupted post-seek HEVC/Dolby Vision frame was caused by starting the compressed video stream on a non-keyframe when a forward seek skipped past the requested target. Forward seek now waits for a safe video keyframe before any video packet is passed to `AVSampleBufferDisplayLayer`, and the regular Matroska demuxer fallback now seeks to a loaded video keyframe instead of an arbitrary packet. Remote seek commit debounce is now 650 ms so several right-arrow presses accumulate into one seek request instead of flushing/rendering repeatedly mid-scrub.
- The slow resume-start log showed `videoPackets` climbing into the thousands while `audioPackets=0`, which means a non-indexed Matroska resume was scanning/decoding from an early cluster instead of jumping near the resume position. Matroska resume now uses a bounded approximate byte seek when cues are absent: it estimates the media offset from duration/file size, reads a small range around that offset, validates real cluster headers by parsing their timecode, and resumes from the closest cluster at or before the target. Initial resume also uses the same keyframe/preroll filter as interactive seek, so old pre-roll packets are skipped before decode instead of being rendered hidden until the target.
- The custom tvOS timeline no longer draws the round white scrubber knob; it now uses a thin vertical playhead marker over the glass bar, matching the Apple reference more closely and avoiding the small dot visible in screenshots.
- HDR/Dolby Vision metadata was previously parsed in some containers but could be dropped before display. The high-risk failures were: Matroska Colour data stopping at demux, MP4 sample-buffer playback requesting 8-bit decoded pixel buffers, sample-buffer HEVC descriptions lacking CoreMedia color extensions, and Direct Play not explicitly requesting the HDR-capable AVKit presentation path. The player now carries HDR metadata through `MediaTrack`, tags HEVC `CMVideoFormatDescription`s, preserves compressed MP4 samples, enables high dynamic range presentation on iOS AVKit, uses automatic preferred display criteria on tvOS AVKit, and sets tvOS sample-buffer `AVDisplayCriteria` from the first rendered video sample.
- The latest tvOS Direct Play failure log showed the progressive MP4/MOV route reaching AVKit `readyToPlay`, then failing in Apple video/caption render pipelines before first-frame telemetry. That points at our AVPlayer instrumentation and startup media selection, not source probing. tvOS HDR/Dolby Vision Direct Play now avoids the 8-bit `AVPlayerItemVideoOutput` probe entirely and does not auto-activate default non-forced embedded subtitles at startup; AVKit keeps full ownership of HDR/DV presentation, and forced/manual subtitle selection remains available.
- The latest tvOS sample-buffer crash log showed `Main Thread Checker: UI API called on a background thread` from `applyPreferredDisplayCriteriaIfNeeded` on `reelfin.nativeplayer.mkv.video`, followed by a BackBoard main-run-loop queue assertion. The fix keeps frame-duration/format-description extraction on the render queue but moves all `UIViewController.view`, `UIView.window`, and `avDisplayManager.preferredDisplayCriteria` mutations into a generation-guarded main-thread coordinator shared by MP4 and Matroska sample-buffer paths.
- Validation for the display criteria threading fix: `xcodegen generate`, focused `NativePlayerConfigurationTests`, tvOS `ReelFinTV` build, tvOS `ReelFinTV` tests, and `git diff --check` passed.
- The latest `2050da6b` Direct Play regression showed a deeper routing bug: the source was Apple-compatible and ultimately selected `AVPlayerViewController`, but the native player had already opened the remote original with `HTTPRangeByteSource`, probed it, selected `MP4Demuxer`, and built an `AVAssetReader` plan. That was not a clean Direct Play startup. Apple-compatible MP4/MOV originals now bypass the native range/probe/demux path completely and return an Apple-native playback snapshot with `nativeProbe=false`, leaving AVKit as the first and only media reader for that Direct Play URL.
- Validation for the Direct Play short-circuit: `xcodegen generate`, focused Direct Play/native routing tests, tvOS `ReelFinTV` build, and `git diff --check` passed. Remaining runtime risk is limited to signed-in hardware/simulator playback against the real Jellyfin URL; the deterministic route tests now assert the exact absence of native probe/demux for compatible MP4/MOV.
- A native player surface preference now makes that Direct Play short-circuit opt-out per server config. The default remains AVKit Direct Play for Apple-compatible originals and the custom sample-buffer player for non-Apple originals; "Always Custom Player" bypasses the AVKit short-circuit, avoids warmed Apple-native selections, and forces compatible MP4/MOV originals through the custom `AVSampleBufferDisplayLayer` route. This is a deliberate diagnostic/compatibility escape hatch, not a new fallback to Jellyfin transcoding.
- Validation for the custom-player force option: targeted red tests failed before implementation and now pass; focused config/routing/settings tests passed 21 tests; focused warmup/native-session/reload tests passed 89 tests; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed.
- The follow-up iOS log showed the UI could display "Always Custom Player" while runtime playback still used `surfacePreference=directPlayWhenPossible`, hit a warmed Apple Direct Play selection, and then fell into the iOS high-risk HLS/AVKit fallback. The root cause was split persistence: the native enable switch wrote `reelfin.nativePlayer.enabled` immediately, but the surface preference only lived in saved server config. The settings UI now exposes one `Native Playback` mode, writes `reelfin.nativePlayer.surfacePreference` immediately, treats `Off` as an explicit runtime override, keeps DEBUG launch migration from re-enabling native mode after the current marker is applied, and the effective native config reads those runtime keys before selecting warmup, AVKit Direct Play, or sample-buffer routing.
- Validation for runtime custom-player persistence: targeted red tests failed before implementation and now pass; focused config/routing/settings tests passed 22 tests and proved a runtime custom preference routes compatible MP4 originals to `nativeplayer.sampleBuffer.route.selected` with `surfacePreference=customPlayer`, no `AVPlayerItem`, and no `AVPlayerViewController`; focused native controller/route guard/warmup/reload tests passed 104 tests; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed.
- The follow-up `2050da6b` iOS logs showed a different Direct Play stability issue: native mode repeatedly cleared warmup, the selected original progressive MP4 route changed between extensionless `/stream` and `/stream.mp4`, first frame arrived, and then Direct Play stalled early before recovery reloaded another static stream. Warmup is now active in native mode and resolves the same original-file Direct Play contract as the native player (`static=true`, `MediaSourceId`, `api_key`, no Jellyfin HLS/transcode query). Guarded progressive Direct Play now gets a bounded Range preheat for resume/high-bitrate startup, the detail surface can mark playback ready from that evidence, and Apple-compatible MP4/MOV originals consistently use `/stream.mp4` while preserving Direct Play instead of profile fallback.
- Validation for native Direct Play warmup/no-alias: `xcodegen generate`, focused 116-test playback/native/detail suite, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed. Remaining runtime risk is server/network-specific: the Range preheat can measure and warm the requested original stream, but real stall elimination still needs a signed-in playback run against the Jellyfin URL and hardware/network path that produced the timeout.
- The next iOS `2050da6b` log showed the startup path was finally healthy on the then-current extensionless `/stream` path: warmup hit, MIME override, `readyToPlay`, first frame at ~2.45s, and verified resume seek. The actual failure was self-inflicted after playback became visible: two `AVPlayerItemPlaybackStalled` callbacks triggered `directplay_poststart_stall` same-route recovery, replaced the working item with another static stream item, and that replacement failed in AVFoundation format loading with `NSURLErrorDomain code=-1`.
- iOS post-first-frame Direct Play stalls now keep the current `AVPlayerItem`, raise `preferredForwardBufferDuration` to at least 24s, keep `automaticallyWaitsToMinimizeStalling`, and reassert play intent so AVPlayer can re-buffer the same stream. Same-route item reload remains available only before first frame on iOS, where replacing the item cannot cut visible playback.
- Validation for iOS post-start Direct Play stall handling: the targeted policy test failed before implementation, then passed after the fix; the focused playback/native/detail suite passed; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed.
- The follow-up iOS log reported audio without image even though `avplayer.first-frame` was logged. The root cause was a false display proof: AVKit sent `readyForDisplay` while the item was still `unknown`, that signal was discarded, then the session accepted decoded pixel-buffer evidence as first-frame proof for an HDR/Dolby Vision Direct Play surface. iOS HDR/DV Direct Play now requires AVKit display readiness before first-frame telemetry, the coordinator rechecks `isReadyForDisplay` after the player reattach, and iOS AVKit is left to negotiate dynamic range instead of forcing `.high` on the controller.
- Validation for AVKit display-proof hardening: the new iPhone HDR Direct Play test failed before implementation on both the readiness requirement and pixel-buffer false positive, then passed after the fix; the ready-for-display forwarding policy test also passed.
- The next iOS log moved the failure from black-video proof to resume ordering: `readyForDisplay` was accepted, but `avplayer.first-frame` was logged at `currentTime=0.000`; only after that did the deferred resume seek jump to `1546.112s`, and playback stalled a few seconds later. That made the visible pipeline start on the beginning of a large progressive HDR/DV MP4 and then seek mid-startup. Direct Play resume now delays first-frame/TTFF until the pending resume position is satisfied, so `ensureDirectPlayResumePositionBeforeAutoplayIfNeeded` performs the preplay seek before user-visible playback is considered started.
- The latest iOS micro-stall logs showed startup was now correctly resumed (`first-frame currentTime=1551.735` / `1584.458`), but autoplay still began with shallow measured buffer (`buffered=3.3`, then `buffered=6.7 likely=true`) and stalled roughly when that buffer drained. That points to an app readiness-policy mismatch rather than a proven server bottleneck. iOS high-bitrate progressive Direct Play now requires a strict 24s measured AVPlayer buffer before autoplay and configures the initial Direct Play forward-buffer target to 24s, so progressive Direct Play no longer starts from a 0s/readyToPlay-only or shallow `likelyToKeepUp` gate.
- Validation for the iOS micro-stall buffer gate: targeted tests failed before implementation on the old shallow-buffer policy and now pass; the focused playback/orientation suite passed; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, `scripts/run_reelfin_player_e2e.sh --skip-ui --skip-tvos --loops 1 --sample-size 4` (artifacts `.artifacts/player-e2e/20260426-222115`), and `git diff --check` passed.
- The follow-up `resume=1590.1s` log exposed a resume deadlock, not a server bottleneck: Direct Play deferred the resume seek until the item became ready, but the pending seek was only applied from the after-first-frame path while first-frame was itself blocked until the resume target was satisfied. That allowed AVPlayer to advance visibly from `0s` (`current=0...11s`) while the app kept waiting for `target=1590s`. The item-ready observer now applies the pending Direct Play resume seek immediately, pauses during that seek, verifies the target, and only then allows first-frame/autoplay.
- Validation for the Direct Play resume-deadlock fix: targeted red test failed before implementation and now passes; the local progressive Direct Play integration test proves `stream` resumes at the requested time before autoplay; focused playback/native/resume suites passed; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `scripts/run_reelfin_player_e2e.sh --loops 2 --sample-size 4 --skip-ui --skip-tvos` passed with artifacts `.artifacts/player-e2e/20260426-232715`. The live UI smoke remains a separate automation issue and is not used as playback correctness evidence.
- The next iOS log showed the resume path was now correct but exposed an over-strict startup gate: `resume_seek phase=item_ready ... satisfied=true`, `avkit.readyForDisplay.accepted`, and `avplayer.first-frame currentTime=1590.417` all happened in ~1.3s, then the paused readiness loop kept waiting for a 24s measured buffer. AVFoundation only reported `buffered=4.6` while paused, so the gate timed out after 24s and called `block_autoplay`, making a visually ready movie appear paused. Startup readiness now accepts first-frame evidence when the item is `readyToPlay` and the pending Direct Play resume target is already satisfied; the 24s forward-buffer policy remains on the player, but it can no longer pause a session after a valid visible frame.
- Validation for the paused-start fix: targeted red test failed before implementation and now passes; focused playback/native/resume/startup suites passed; `xcodegen generate`, iOS `ReelFin` simulator build, tvOS `ReelFinTV` simulator build, and `scripts/run_reelfin_player_e2e.sh --loops 1 --sample-size 4 --skip-ui --skip-tvos` passed with artifacts `.artifacts/player-e2e/20260427-205533`.
- The iOS Play/Resume screenshot regression was a presentation ordering bug: Detail/Home requested `UIWindowScene` landscape geometry before the full-screen player existed, so the Detail surface itself briefly rotated into a landscape canvas. The pre-cover step now leaves the current orientation mask and geometry stable; the actual landscape lock/request happens from `PlayerView.onAppear`, after the black/player surface is mounted.
- The latest iOS Direct Play reliability reset reclassified the remaining failures. The server was not the bottleneck in the supplied logs: live range benchmarks for the 21.8 Mbps MP4 source were far above source bitrate, and the app reached `readyToPlay`, resume satisfaction, and first-frame quickly. The failure was app-side lifecycle handling: post-first-frame Direct Play item failure was suppressed, so an `AVFoundationErrorDomain -11819` item could never recover. `DirectPlaySessionPolicy` now makes Direct Play recovery rules explicit, allows bounded same-route recovery for failed Direct Play items after first frame, and keeps the stricter suppression for non-Direct Play post-start failures.
- The same reset fixed the user-visible start/resume contract. `PlaybackStartPosition.beginning` bypasses server/local resume progress when the Detail primary action is `Play`; `Resume` remains progress-aware. MP4/MOV original URL construction now emits `/stream.mp4` at resolver time instead of a late alias, which keeps AVFoundation on a stable progressive MP4 endpoint without changing to transcode.
- Validation for the clean Direct Play reset: focused playback/native/detail/orientation suite passed 128 tests; the live player E2E passed with artifacts `.artifacts/player-e2e/20260427-211642` (`4/4` explicit probes, `4/4` original-stream benchmarks, `3/3` live playback probes, `69/69` deterministic tests, clean runtime-log scan); tvOS `ReelFinTV` simulator build passed. UI smoke and real HDR/Dolby Vision display proof remain hardware/manual validation items.
- The final iOS `2050da6b` runtime pass showed that waiting for progressive Direct Play to prove itself was still the wrong product behavior for auto quality: the source is high-bitrate HDR/Dolby Vision-class MP4 with E-AC3 audio, so AVFoundation can display it but the progressive path remains fragile under simulator/live Jellyfin conditions. Auto-quality iOS now preempts that exact high-risk shape to `forceH264Transcode` HLS before playback starts. Strict quality, Direct/Remux-only, and tvOS paths still preserve Direct Play.
- Validation for the iOS preemptive HLS fallback: targeted red tests failed before implementation and now pass; full `PlaybackEngineTests` completed 515 tests with 512 passed, 3 skipped, and 0 failures; XcodeBuildMCP `build_run_sim` succeeded on iPhone 17 Pro iOS 26.0; tvOS `ReelFinTV` simulator build passed; Computer Use playback of the real Jellyfin item produced moving screenshots over roughly one minute; the redacted runtime log `.artifacts/manual-player-debug/20260427-final-224605/runtime.redacted.log` shows `preemptive_fallback`, `route=Transcode (HLS)`, and no real app-level stall, timeout, watchdog, route-guard block, or AVPlayer failure markers.
- The follow-up iOS resume log exposed a split-brain HLS resume path: ReelFin selected `resume=857.9s`, but the final normalized Jellyfin HLS URL could still be missing `StartTimeTicks`, so the server-side playlist could start at zero even while the UI showed Resume. Transcode/remux asset URLs now append `StartTimeTicks` after URL normalization and before API-key injection; Direct Play/native routes remain unchanged and use their existing client-side seek path.
- Validation for the HLS resume URL fix: targeted red test failed before implementation and now passes; focused route/policy/session tests passed 162 tests; full `PlaybackEngineTests` passed 523 tests with 3 skips and 0 failures after `MockMetadataRepository` was made actor-isolated to remove a concurrent test data race; live player E2E passed with artifacts `.artifacts/player-e2e/20260428-194046` (`4/4` explicit probes, `4/4` original-stream benchmarks, `3/3` live playback probes, `71/71` deterministic player tests, live iOS UI smoke, runtime log cleanliness pass).
- Superseding note for the HLS resume URL fix: passing `StartTimeTicks` through to AVPlayer's final master/variant URL can make Jellyfin segment URLs unavailable after resume and surface as `NSURLErrorDomain -1008`. Resume ticks now stay in the PlaybackInfo request only; final HLS asset URLs and pinned variants strip resume query parameters, and the controller decides whether to apply an absolute transcode offset from the loaded stream duration.
- The new dynamic original-first policy supersedes the metadata-only iOS high-risk HLS preemption. Progressive MP4/MOV Direct Play is now attempted when the measured Range preheat proves enough headroom, guarded with short startup windows when usable, and profile-recovered only when the preheat is missing or below the source-bitrate headroom threshold. This keeps quality high on good networks without returning to the earlier 24s startup gate.
- Validation for the dynamic original-first Direct Play policy: targeted red tests failed before implementation and now pass; focused playback/native startup tests passed 202 tests; `xcodegen generate`, iOS `ReelFin` simulator build, and tvOS `ReelFinTV` simulator build passed; lightweight live player E2E passed with artifacts `.artifacts/player-e2e/20260504-213021` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `4/4` live playback probes, `76/76` deterministic tests, clean fatal-log scan; UI and tvOS runner steps intentionally skipped by the fast variant). Remaining runtime risk is hardware-specific HDR/Dolby Vision display proof.
- An earlier tvOS Direct Play tuning superseded the high-bitrate network-headroom shortcut by forcing the 24s no-stall forward-buffer policy for premium progressive MP4/MOV. The later fast-start pass below supersedes that behavior again for Apple-compatible progressive Direct Play, because the real `2050da6b` logs showed startup was being self-gated rather than protected.
- The earlier pass targeted the fragile premium/high-bitrate shape without changing iOS or forcing tvOS into HLS/transcode; the current behavior is documented in the following fast-start entries.
- Validation for the tvOS Direct Play no-stall correction: the high-bitrate network-headroom regression test failed before the policy change and now passes; the focused PlaybackEngine playback/startup suite passed 94 tests; `ReelFinTV` build and tests passed on Apple TV 4K (3rd generation) tvOS 26.2 simulator; live player E2E passed with artifacts `.artifacts/player-e2e/20260428-202226` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `3/3` live playback probes, `73/73` deterministic player tests, tvOS build gate, clean fatal-log scan).
- The latest tvOS `2050da6b` Direct Play log showed the cut/crash recovery path was gone, but TTFF was still almost 4s because startup was self-gated by `tvos_high_bitrate_directplay_ready` and a forced `24s` no-stall buffer policy. Product behavior for Apple-compatible MP4/MOV Direct Play is now intentionally simple: create the AVPlayer item, apply resume when the item becomes ready, and start. tvOS progressive Direct Play no longer creates a startup readiness requirement, startup-blocking range preheat, autoplay block, or forced 24s forward-buffer override.
- The follow-up tvOS `2050da6b` Direct Play log removed the startup gate but still showed `first_frame.deferred_until_resume` while AVPlayer advanced near `0s`; the app was allowing autoplay before the resume seek had been attempted. tvOS MP4/MOV Direct Play now primes the resume seek immediately after item replacement and before autoplay, clears the pending resume once the target is satisfied, and keeps the background Detail range preheat as opportunistic seek priming rather than a startup gate.
- AVKit readiness and native sample-buffer lifecycle were hardened at the same time: a tvOS `readyForDisplay` signal that arrives before `AVPlayerItem.readyToPlay` is rechecked when the item becomes ready, and MP4/Matroska sample-buffer tasks are generation-guarded so stale async work cannot render, publish diagnostics, or call final completion after reload/stop.
- Validation for the fast tvOS Direct Play pass: the targeted playback policy tests failed first on the old startup gate and 24s buffer behavior, then passed after the change; focused `NativePlayerConfigurationTests` passed 24 tests; `xcodegen generate`, `ReelFinTV` build/test, and live player E2E passed with artifacts `.artifacts/player-e2e/20260503-105130` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `3/3` live playback probes, `76/76` deterministic player tests, tvOS build gate, clean fatal-log scan).
- The latest tvOS post-start log showed the fast-start contract was fixed (`resume_seek phase=initial ... satisfied=true`, first frame at the resume point), then playback stalled only after the adaptive caching ramp raised progressive Direct Play buffering from `24s` to `90s`. That ramp treated the requested forward-buffer target like real buffered media and could push AVPlayer into repeated waits after a visible frame. tvOS progressive MP4/MOV Direct Play now skips the adaptive caching ramp entirely, preserves the current playback intent while AVPlayer reports `waitingToPlayAtSpecifiedRate`, and handles post-first-frame stalls by keeping the current item without same-route reload, buffer target growth, or `automaticallyWaitsToMinimizeStalling`.
- Validation for the no-pause tvOS Direct Play pass: targeted tests failed first against the missing playback-state/adaptive-caching/stall-policy contracts and then passed after implementation; focused playback/native/startup/warmup/detail tests passed 154 tests; `xcodegen generate`, `ReelFinTV` build/test, and live player E2E passed with artifacts `.artifacts/player-e2e/20260503-110443` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `3/3` live playback probes, tvOS build gate, clean runtime-log scan).
- The follow-up rollback pass supersedes the previous post-start stall behavior. The latest tvOS logs proved Direct Play startup itself was correct: first frame landed at the resume point, then the app's own fallback chain replaced a visible Direct Play item with HLS/transcode after repeated stalls. `directplay_poststart_stall` is no longer a recovery-triggering failure, no longer disables direct routes, and is blocked even if passed as an explicit native coordinator fallback reason.
- Current post-first-frame Direct Play behavior is deliberately simple: keep the same `AVPlayerItem`, reassert play intent, enable `automaticallyWaitsToMinimizeStalling` only after the visible stall, and grow tvOS forward-buffer targets dynamically from `24s` to `60s`, `120s`, then `240s` for repeated stalls. Startup remains fast; continuity tuning starts only after a real post-start stall.
- Validation for the fallback rollback and dynamic cache pass: `xcodegen generate` passed; `PlaybackSessionControllerTrackReloadTests` passed 76 tests; `PlaybackPolicyTests` passed 56 tests; `NativePlayerRouteGuardTests` passed 12 tests; `ReelFinTV` simulator build passed; live player E2E passed with artifacts `.artifacts/player-e2e/20260503-114654` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `3/3` live playback probes, `74/74` deterministic tests, clean runtime-log scan).
- The quality-first measured-original pass keeps Direct Play as the default again, but removes the blind metadata-only shortcut. Foreground warmup can now collect a short-lived server Range baseline through the same authenticated original-file route, while Detail/Home/Library item warmup keeps the stronger resume-specific preheat. Fresh high-headroom evidence skips startup gates; stale or missing evidence falls back to a short guarded Direct Play start rather than HLS/transcode; weak measured evidence remains the only prestart network reason to profile-recover away from original quality.
- Server baseline probes are intentionally bounded and privacy-safe: they require a partial-content response, read only the requested byte window, cache one result per server/network/platform scope, single-flight concurrent callers, and log only item prefixes plus byte/timing/bitrate fields. Post-first-frame stalls still keep the visible `AVPlayerItem`; repeated stalls mark the exact Direct Play route fragile for a short TTL so the next attempt waits for stronger item-specific proof before trusting the global baseline.
- Validation for the measured-original pass: targeted red/green policy and warmup tests passed; the focused playback/native suite passed 211 tests; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed; live player E2E passed with artifacts `.artifacts/player-e2e/20260505-100500` (`4/4` explicit probes, resume reporting, `4/4` original-stream benchmarks, `4/4` live playback probes, `76/76` deterministic tests, clean runtime-log scan).
- Remaining HDR/Dolby Vision risk: final tone mapping and display-mode switching still require hardware validation on an HDR/Dolby Vision Apple TV or iPhone display. Dolby Vision profile detection is present, but full codec-private-data/RPU parsing and custom sample-buffer Dolby Vision enhancement handling are still bounded by what Apple accepts through tagged HEVC sample buffers.
- Remaining performance risk: compressed E-AC3 renderer behavior still needs validation on real Apple TV hardware and a signed-in Jellyfin runtime session; simulator HAL audio errors are noisy and not enough to prove hardware continuity. If cuts persist while `audioRebuffers` stays flat and Dolby multi-frame timing is active, or if `nativeplayer.audio.startup_degraded` appears for playable Dolby tracks, the next implementation step is a decoded PCM `AudioConverter`/`AVAudioEngine` path for E-AC3 rather than compressed sample-buffer audio. Matroska forward seeks are now faster when the target is ahead of the active stream position, but backward seeks and non-indexed far jumps still need either Matroska cues or a real demuxer byte-seek index for instant random access.

## Playback Quality Guarantees And Startup Policy - 2026-05-07

- Route quality is now represented explicitly instead of inferred from Jellyfin `PlayMethod` labels. `PlaybackRouteGuarantees` distinguishes original bitstream, video-copy remux, audio-only transcode, video transcode, HDR10/HLG/Dolby Vision/SDR tone map, and startup route class.
- Dolby Vision classification now separates Profile 5, 8.1, 8.4, Profile 7, unknown, and none. The resolver only promises Dolby Vision when the source and route evidence support it; Profile 7 and missing HLS/init DV boxes are reported as fallback/unknown instead of as guaranteed DV.
- The route order now favors AVKit Direct Original, then progressive/fMP4 video-copy remux, then experimental native direct where configured, and only then video transcode. Automatic recovery and stored fallback profiles no longer silently force 4K HDR/Dolby Vision into `appleOptimizedHEVC` or `forceH264Transcode`; destructive or unknown-preservation routes publish fallback recommendations.
- Startup tuning moved into `PlaybackStartupPolicy`: direct/local/LAN/original routes use short buffers and guarded immediate start, remote direct uses about 2s, HLS remux about 3s, and video transcode about 5s. `PlaybackStartupTrace` records TTFF checkpoints for PlaybackInfo, route selection, asset/item creation, ready, first frame, play start, and first stall.
- `PlaybackHealthMonitor` records repeated stalls, startup slowness, route failure, observed bitrate, required bitrate, and safety ratio. These diagnostics can recommend keeping original quality or choosing smoother lower-bitrate playback without auto-downgrading the route.
- LocalMediaGateway now preserves upstream content type, validates range forms, returns correct 206/416 range headers, records throughput, and avoids exposing secret-bearing remote URLs. The gateway still does not transform original media or buffer whole files.
- Remaining confidence limit: simulator tests can prove routing, diagnostics, and absence of silent video-transcode decisions, but they cannot prove actual HDR/Dolby Vision panel output. That still requires signed-in playback on HDR/Dolby Vision-capable Apple hardware with route diagnostics captured.

## iOS High-Bitrate Direct Play Startup Gate - 2026-05-08

- Runtime evidence from the `2050da6b` logs: preheat and server baseline timed out, AVKit reached `readyToPlay`, startup accepted first-frame evidence at `currentTime=0` with about 2s buffered, and playback stalled around 11.8s. That made the failure an app startup-gate mismatch, not proof that HDR/Dolby Vision Direct Play had to be downgraded.
- Fix: iOS high-bitrate progressive Direct Play now starts only after 24s of measured AVPlayer buffer, waits up to 30s, does not autoplay on readiness timeout, and no longer treats first-frame evidence as sufficient for fresh or resumed Direct Play startup.
- Follow-up evidence from the newer log: preheat/server baseline were fast, so readiness skipped via `directplay_measured_headroom`; AVPlayer reached `readyToPlay`, resume seek was satisfied, then playback still stalled repeatedly around 12-18s. That proved synthetic range throughput and a visible resume frame are not enough. iPhone high-bitrate progressive Direct Play now treats even excellent preheat/server-baseline headroom as guarded, so only measured AVPlayer `loadedTimeRanges` can start autoplay.
- Post-start iOS Direct Play stalls no longer call `play()` immediately after raising the forward buffer. The expected runtime marker is now `playback.directplay.poststart_rebuffer.wait ... action=pause_current_item`, followed by `poststart_rebuffer.ready ... action=resume_current_item` only after measured AVPlayer rebuffer. tvOS keeps the existing keep-current-item waiting behavior.
- Validation: targeted red startup/readiness/session tests failed before implementation and now pass; focused Direct Play startup/readiness/session tests passed 30 tests; full `PlaybackSessionControllerTrackReloadTests` passed 87 tests; `xcodegen generate`, iOS `ReelFin` build, tvOS `ReelFinTV` build, and `git diff --check` passed.
- Follow-up validation: new regression tests for guarded iPhone headroom, first-frame resume bypass, and iOS post-start measured rebuffer passed; focused Direct Play startup/readiness/session tests passed 120 tests. Full build gates still need to be rerun after this follow-up.
- Remaining runtime risk: signed-in playback of the exact Jellyfin item on the target iPhone/network is still needed to prove wall-clock continuity and actual HDR/Dolby Vision panel output.

## macOS Catalyst And Live Player Validation - 2026-05-09

- Mac Catalyst is now generated from `project.yml` for the existing `ReelFin` scheme, keeping iOS/iPad/tvOS architecture intact while allowing a dedicated Mac root shell and a local shell build/run path.
- The 2026-05-10 follow-up replaces the temporary iPad-style Catalyst split with a Mac-specific sidebar-detail shell, scene-restored selection, toolbar controls, and Command-menu shortcuts. This keeps tvOS on `TVRootShellView` and leaves iPhone/iPad layout policy explicit.
- The final live-player loop found one real app-side blocker after a successful first frame: Direct Play startup readiness could still time out after a resumed frame was visible. The fix keeps the stricter Direct Play startup gate for inactive sessions, but lets an actively requested, ready, resume-satisfied Direct Play session finish readiness from first-frame evidence.
- Validation passed for `xcodegen generate`, Mac Catalyst launch/process/log verification, focused Mac Catalyst tests, the full live Jellyfin player E2E with artifacts `.artifacts/player-e2e/20260509-110718`, tvOS build gate, and runtime-log cleanliness. Mac Catalyst XCUITest UI traversal remains blocked by Xcode automation initialization before ReelFin receives interactions, so the real UI player traversal proof for this run comes from the iOS live UI smoke plus live probes rather than macOS XCUITest.
- 2026-05-10 validation passed: focused `RootDesktopLayoutPolicyTests`, `scripts/run_reelfin_macos.sh --verify`, and `ReelFinTV` simulator build.

## tvOS Player Input/Chrome Audit - 2026-07-11

- Removed competing `AVPlayerViewController` playback controls and `pressesBegan` interception from the CustomPlayer tvOS route. Play/Pause now has one SwiftUI command path; Select and directional input have one focusable transparent transport surface while chrome is hidden.
- Remote seek previews accumulate against the pending target and commit after a 280 ms latest-wins debounce. Both playback routes clamp targets to zero/title duration; NativePlayer keeps its generation-guarded seek request path.
- The timeline is no longer an empty-action button: it consumes Left/Right, publishes position accessibility state, and Select hides chrome. A hidden input surface has its focus/hover effects disabled so focus cannot render a full-screen white plate.
- Simulator evidence: authenticated launch and real Jellyfin playback were preserved with update-install only; a live Device Hub pass exposed the white focus plate, which was corrected. Deterministic gates passed 18/18 chrome/remote tests plus 33/33 NativePlayer configuration tests, followed by clean iOS/tvOS builds.
- Remaining validation: Task 5 must add a tvOS UI-test target and prove Play/Pause, Select, repeated Left/Right, panel focus restoration, Menu precedence, advancing video, and audio evidence with `XCUIRemote`.

## tvOS Native Liquid Glass Player Chrome - 2026-07-11

- Replaced the intermediate text-heavy action row with the native tvOS reference hierarchy on both CustomPlayer and NativePlayer: episode/title metadata at the lower left, circular Subtitles/Audio/Video controls at the lower right, a full-width timeline, and Info/InSight/Continue Watching glass capsules below it.
- The chrome uses tvOS 26+ interactive Liquid Glass primitives for controls and keeps iOS layout behavior unchanged. Info and Video expose route/quality facts already known by the active player; InSight is deliberately limited to Jellyfin metadata and does not claim scene or people recognition.
- Focus and Menu behavior remain deterministic: utility actions participate in the same focus model, Continue Watching resumes paused playback and hides chrome, and Menu closes an open information/track panel before hiding chrome or dismissing playback.
- Authenticated simulator evidence on Star City S1E1 passed the complete Liquid Glass journey in 49.3 seconds, including hidden-chrome recovery, Info, InSight, track/video panels, Continue Watching, focus restoration, and Menu precedence. Captures were exported to `/tmp/ReelFinLiquidGlassAttachments2`.
- Remaining validation belongs to the hardened live Task 5 suite: ensure opt-in tests cannot mutate Jellyfin progress, assert Continue/Restart start positions, perform real track changes, and make advancement evidence expire on pause or freeze.

## tvOS Hermetic Live Player Validation - 2026-07-11

- Closed the Task 5 validation gaps without changing the Liquid Glass layout. Live automation anchors exist only for the exact DEBUG tvOS opt-in; the default ReelFinTV test run skips the Jellyfin journeys cleanly.
- The test process now passes only `star-city-s1e1`. The authenticated app resolves Star City season 1 episode 1 through its existing API client, uses a local seven-minute fixture position, and suppresses every Jellyfin/local watched-progress mutation for CustomPlayer and legacy/native routes.
- Fixed the NativePlayer paused-Continue race, separated the real Info destination from Video, localized `Continuer`, and renamed the metadata-only `InSight` action to the honest `Détails` label.
- Deterministic validation passed 27 focused tests covering evidence expiry, accepted audio samples, exact opt-in policy, minimum loop count, paused Continue, and persistence isolation. tvOS simulator/default-skip and authenticated live results are recorded only as redacted ignored artifacts under `.artifacts/player-e2e/`.
- Live Star City S1E1 found two release blockers that deterministic metadata checks alone missed: the preferred MP4 twin carried codec-less subtitle placeholders masking its MKV twin's real SRT metadata, and disabling the entire chrome focus scope permanently cleared tvOS `FocusState` after closing a panel. Source merging now upgrades placeholders while retaining the delivery source ID; the chrome keeps a stable focus candidate graph and gates actions instead of disabling or remounting its scope.
- The final focus graph is automation-proven: Video -> timeline -> Info vertically, bounded horizontal movement in both rows, and exact origin restoration after Video, Info, and Détails. Every visible action is a real native Glass Button; unavailable subtitle/audio actions are omitted rather than rendered inert.
- Final evidence: 61/61 focused tests; signed authenticated Star City S1E1 journeys passed in 55.857s (tracks/panels/focus/paused Continue), 46.498s (resume, stable pause, seek to zero, forward seek, no crash), and 135.492s (10 alternating Continue/Restart loops). `xcodegen generate`, iOS build, tvOS build, `git diff --check`, and default opt-out execution (3 skipped, 0 failures) passed.
- Final native-UI refinement adapted the iOS track-list hierarchy to a compact tvOS Liquid Glass popover: 460-point right-side panel, 58-point rows, no opaque black fill, no full-white selected capsule, structured Jellyfin labels, selected-state checkmark, and a distinct translucent focus state. The updated panel passed 27 focused tests and a 56.687-second authenticated Star City journey; the complete ReelFinTV gate then passed 2 local tests plus all 3 live journeys in 237.375 seconds.
- Task 5 AVKit-faithful menu proof followed a live RED/GREEN loop on the preserved authenticated simulator. The first marker test failed as expected at `native_player_avkit_subtitles_root` after 40.609s; the visual review then rejected a Language page with zero selected checks, and the added assertion failed 0-vs-1 after 27.101s. The retained-language check now reflects the preferred activatable subtitle track while Off, without changing playback selection.
- A second strict review rejected the fixed `ScrollView.frame(maxHeight: 520)` because Audio, Language, and Style expanded into a large dark empty card. `NativePlayerAVKitMenuLayout` now computes content height from real choice/navigation/divider extents and 2-point spacing, caps it at 520 only when scrolling is required, and otherwise hugs content. Red compilation proved the new contract was absent; the three focused tests then passed 3/3 in 0.006s for Audio four rows = 278, Style two rows = 138, and a long list = 520.
- The final compact authenticated menu journey passed 1/1 in 96.697s. It proved four exact page markers and attachments, real focus before Select, one language change, both subtitle background styles, one audio-track change, playback continuation/advancement after every selection, focus restoration, Video/Info/Détails destinations, paused Continuer, and zero player errors. The final `.xcresult` is `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-compact-live-20260712.xcresult`.
- Final strict visual outcome against the user Audio/Subtitles references: accepted. All four cards transmit the underlying frame through Liquid Glass; no opaque fill, generic system list, full-white focus capsule, hidden/clipped text, or large empty tail remains. Audio, Off, retained Language, and Style show their expected checkmarks; Language rows preserve secondary metadata; the root preserves selected-language/style details and both chevrons. Final PNGs are `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-avkit-compact-final-20260712/28B9B261-4401-4DE0-8A19-3BC752909037.png`, `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-avkit-compact-final-20260712/00DB0AAC-5F5F-4D1A-A3AB-187D467DC2B0.png`, `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-avkit-compact-final-20260712/88642741-3EA7-4F5C-A72E-C98BF28E5201.png`, and `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-avkit-compact-final-20260712/F3760368-232E-4F65-AE2E-CF5E2D3EE86D.png`.
- Final `ReelFinTV` gate passed 2/2 local tests and 3/3 live tests with zero failures: menus 96.235s, pause/resume/seek 46.629s, and 10 alternating Continue/Restart cycles 138.784s; live suite 281.649s and total xcodebuild 287.775s. Result bundle: `/Users/flo/Documents/Projet/ReelFin/.artifacts/player-e2e/task5-full-final-20260712.xcresult`. `xcodegen generate` and `git diff --check` passed. Simulator audio proof is limited to accepted renderer/route state and an advancing clock, not audible output or HDMI format negotiation; simulator HDR/Dolby Vision proof is limited to route/metadata/app behavior, not display-mode switching, tone mapping, or final luminance on compatible hardware.

## Final Player Review Corrections - 2026-07-12

- Adaptive prewarm/resume: focus-dwell resolution may still remove the PlaybackInfo latency for progressive/native routes, but adaptive results are rejected at both cache insertion and consumption. Resume uses a distinct tick-aware resolver key and an exact-tick Play resolution.
- Resolver concurrency/auth isolation: `ResolutionMemo` is an actor with one atomic claim operation and owner-ID-checked completion. Its typed key includes item, audio/subtitle signatures, resume ticks, and a non-logged authenticated server/user/session scope, preventing duplicate POSTs, removal races, and cross-session reuse of signed material.
- Subtitle lifecycle: route-owned memory survives Off -> dismiss -> reopen; custom sidecars update it only when `activeTrackID` is confirmed after parsing. Default/Défaut precedes Forced/Forcé, including labels carried in the title when no badge exists.
- Sidecar failure and logging: failed downloads/parses clear pending state, never confirm the failed ID, and enter the established player failure path. Track-selection logs emit status only, with regression scanning for raw IDs, signed URLs, headers, tokens, and API keys.
- Verification on the available Xcode-beta Simulator 27.0 runtimes: combined focused suite passed 238/238 with zero failures/skips; `xcodegen generate` passed; `ReelFin` iOS and `ReelFinTV` tvOS builds both exited 0; `git diff --check a7e1f7a` passed. Existing Swift 6/deprecation warnings outside this correction scope remain non-fatal.

## tvOS UX Polish Final Audit - 2026-07-12

- Authenticated focus and transition proof covers Home landscape/poster shelves, Library horizontal travel, compact Resume choice, exact detail-source restoration, rapid Back, and playback chrome continuity. Exported frames show no clipping, title collision, offscreen focus, or hard black transition.
- A release-blocking duplicate-focus defect was found and fixed. Home cards now use row-qualified transition-source IDs, return targets retain their original row source, and automatic Hero fallback cannot cancel an active exact-card handoff.
- Glass and layer review found no repeated per-frame glass construction: Library applies interactive glass only to the focused cell, and player chrome remains grouped. No extra compositing layer, timer, display link, or per-frame allocation was introduced.
- Concurrency review found no new detached work, broadened `MainActor` isolation, unbounded task, or uncanceled lifecycle work. Existing view tasks remain canceled on disappearance, and focus handoff stays event-driven rather than sleep-driven.
- `run_player_ui_probe.sh` produced zero successful checks: both selected tests were credential-skipped (2/2 skipped in 79.466 seconds). Authenticated tvOS journeys provide the compensating live proof. `run_playback_qa_loop.sh` passed 970 PlaybackEngine tests with ten expected skips in 411.725 seconds and six ImageCache tests in 0.922 seconds. Outer probe/QA command durations were not captured in their logs. Runtime logs contained no Main Thread Checker, Core Animation background-thread, player-error, or unexpected stall evidence.
- Final gates passed: 95/95 focused iOS tests, 2/2 local tvOS tests, 8/8 authenticated tvOS journeys, `xcodegen generate`, and both platform builds. No launch, focus, playback-startup, or cache release blocker remains.
- Device Hub cannot synthesize circular clickpad coordinates, and the standalone profile script requires credentials intentionally absent from the environment. Physical remote feel, audible HDMI output, negotiated formats, HDR/Dolby Vision display switching, tone mapping, and final luminance remain hardware-only concerns.

## tvOS Focus Evidence Review Follow-up - 2026-07-12

- The new focus-transition evidence is a local value counter, not an observer. Its mutation and 1×1 UIKit accessibility marker exist only for DEBUG tvOS processes explicitly launched with the automation flag; the marker publishes only the count.
- The counter records only unequal `focusedHomeItemID` values. Authenticated proof requires one Down event to produce one distinct stable focused card and an exact settled count delta of one; the focus/Home-Back/Library-Back suite passed 3/3 in 63.011 seconds, and rapid Back passed 1/1 in 11.225 seconds.
- Mandatory focus acquisition no longer converts a regression into an XCTest skip. Preparation evidence is captured after its marker exists. No production timer, detached task, lifecycle observer, media payload, or broad release-build focus observation was added.

## tvOS Detail Return Provenance Follow-up - 2026-07-12

- Return resolution is event-driven and bounded to the originating rail. It performs linear work over the small rail snapshot only on a displayed-item change or dismissal, introduces no timer, detached task, network request, artwork fetch, or per-frame allocation, and preserves the existing cancelable focus handoff.
- Row-qualified focus IDs remain the sole automatic row focus target. Hero is considered only for Hero-origin presentations; a missing row target selects the nearest surviving snapshot item in that row and never consults the global first-row item map.
- Motion and Resume changes are constant layout/animation tokens. Reduce Motion selects a 0.18-second ease-out in `TVMotionFocusModifier`; normal focus selects the 0.28/0.80 spring. Library activation is reduced to 1.025, with no additional glass layer or observation scope.
- Verification passed 38/38 focused deterministic tests and the ReelFinTV build on the authenticated simulator. The nearest available live Home Back test passed twice consecutively after one initial transient timeout. No carousel-driving UI test exists in the suite; duplicated-rail, Hero-origin, neighbor navigation, removed-target fallback, and unique row-plus-item focus are covered deterministically.

## TestFlight Focus Ownership And Auth Gate - 2026-07-13

- The TestFlight gate exposed two release blockers before archive: authenticated HTTP 401 responses returned `unauthenticated` without clearing the rejected persisted session, and tvOS cards could bind the same focusable view to both local and parent `FocusState` values. The auth path now compares the failed request-token snapshot before clearing state, while Home/Library/Search each have one explicit focus owner.
- Focus styling remains constant-time state derivation from the parent focus ID. No timer, observer, detached task, glass layer, network request, or broader `MainActor` scope was added. Removing the competing local binding also removes redundant focus invalidations.
- Deep-row Detail return now ignores automatic intermediate focus changes during the scroll animation. User cancellation remains event-driven through `onMoveCommand`; the generation-owned handoff still cancels stale completions. Five consecutive authenticated Home return repetitions passed after this correction, compared with three failures in five before it.
- XCUI's outgoing/incoming focus snapshots are treated as an observation artifact only while exactly two snapshots overlap. The helper resets its stability counter and still requires three unique observations; counts above two fail immediately, and persistent ambiguity times out.
- Final evidence: JellyfinAPI 45/45, the complete iOS scheme with zero failures, 2/2 local tvOS tests, and 8/8 authenticated Star City S1E1 tvOS journeys in 384.565 seconds (391.100-second Xcode operation). The release candidate preserves the simulator session and produced no player error during the ten-cycle reliability journey.

## Unified iPhone And Apple TV Release Boundary - 2026-07-13

- The unified release retains one App Store record by keeping `PRODUCT_BUNDLE_IDENTIFIER = com.reelfin.app` for both application schemes, while the binaries are now explicitly limited to iPhone and Apple TV. iOS uses `iphoneos iphonesimulator`, device family `1`, and `NO` for Mac Catalyst, Designed for iPhone/iPad on Mac, and Designed for iPhone/iPad on visionOS; tvOS uses `appletvos appletvsimulator` and device family `3`.
- The release preflight gained regression checks for all of those boundaries. Its RED run exited 1 with the five expected missing/re-enabled-platform failures while the existing device-family checks stayed green; after the XcodeGen restrictions, generation and every preflight assertion passed.
- Effective Release settings independently confirmed version `0.1.1`, build `12`, the shared application bundle identifier, and the expected platform/device-family values for `ReelFin` and `ReelFinTV`. This configuration-only change adds no launch, focus, playback, cache, asynchronous, or `MainActor` work.
